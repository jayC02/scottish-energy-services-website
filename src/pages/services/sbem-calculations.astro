---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServiceDetail from '../../components/ServiceDetail.astro';
import { services } from '../../data/services';
const service = services.find((s) => s.slug === 'sbem-calculations')!;
---
<BaseLayout title="SBEM Calculations" description={service.short}><ServiceDetail service={service} /></BaseLayout>
