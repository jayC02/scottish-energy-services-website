---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServiceDetail from '../../components/ServiceDetail.astro';
import { services } from '../../data/services';
const service = services.find((s) => s.slug === 'fras')!;
---
<BaseLayout title="Fire Risk Assessments (FRAs)" description={service.short}><ServiceDetail service={service} /></BaseLayout>
