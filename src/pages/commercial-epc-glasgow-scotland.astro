---
// Retained for existing links and campaigns; primary search URL is the service page.
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceDetail from '../components/ServiceDetail.astro';
import { getServicePage } from '../data/service-pages';
const { service, detail, schema } = getServicePage('commercial-epcs');
---
<BaseLayout title={detail.title} description={detail.description} canonical="/services/commercial-epcs" structuredData={schema}>
  <ServiceDetail service={service} />
</BaseLayout>
