<section class="section hero">
  <video class="hero-video" autoplay muted loop playsinline aria-hidden="true">
    <source src="/videos/hero-background.mp4" type="video/mp4" />
  </video>
  <div class="hero-overlay" aria-hidden="true"></div>

  <div class="container hero-grid">
    <div data-reveal>
      <p class="kicker">Energy, Compliance & Building Performance</p>
      <h1>Commercial EPCs, SAP, SBEM & Fire Risk Assessments Across Scotland</h1>
      <p class="lead">Fast, accredited compliance reports for landlords, agents, architects, developers and commercial property teams. Based in Glasgow, supporting projects across Scotland and the UK.</p>
      <div class="actions">
        <a class="btn btn-primary" href="/quote">Request a Quote</a>
        <a class="btn btn-outline" href="tel:01412551360">Call 0141 255 1360</a>
        <a class="btn btn-ghost" href="/services">Explore Services</a>
      </div>
    </div>
  </div>

  <div class="hero-fade" aria-hidden="true"></div>
</section>

<style>
.hero{padding-top:5.7rem;position:relative;overflow:hidden;background:#f2f6fb;border-bottom:1px solid rgba(205,218,231,.42)}
.hero-video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:-3}
.hero-overlay{position:absolute;inset:0;z-index:-2;background:rgba(0,0,0,.35)}
.hero-grid{position:relative;z-index:1;display:grid;grid-template-columns:1fr;max-width:900px}
h1{color:#fff;font-size:clamp(2rem,4.5vw,3.5rem);line-height:1.08;margin:.7rem 0 1.1rem;text-shadow:0 5px 24px rgba(6,15,25,.45)}
.kicker,.hero :global(.lead){color:#eef6ff}
.actions{display:flex;flex-wrap:wrap;gap:.8rem;margin-top:1.45rem}
.hero-fade{position:absolute;inset-inline:0;bottom:0;height:clamp(120px,18vw,190px);background:linear-gradient(to bottom,rgba(242,246,251,0),#fff 84%)}
</style>
