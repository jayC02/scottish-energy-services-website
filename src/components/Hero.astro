<section class="section hero">
  <div class="hero-motion" aria-hidden="true">
    <canvas id="hero-field"></canvas>
    <div class="hero-grid-fade"></div>
  </div>
  <div class="container hero-grid">
    <div data-reveal>
      <p class="kicker">Energy, Compliance & Building Performance</p>
      <h1>Scottish Energy Services</h1>
      <p class="lead">Based in Scotland and delivering projects across the UK, we support residential and non-domestic clients with disciplined assessments, clear reporting, and dependable communication.</p>
      <div class="actions">
        <a class="btn btn-primary" href="/quote">Request a Quote</a>
        <a class="btn btn-ghost" href="/services">Explore Services</a>
      </div>
      <ul class="hero-points">
        <li>Accredited technical services for projects throughout the UK</li>
        <li>Commercially focused advice with practical implementation value</li>
        <li>Fast coordination and report quality you can rely on</li>
      </ul>
    </div>
    <div class="hero-panel" data-reveal>
      <div class="metric"><strong data-count="3200">0</strong><span>Projects delivered</span></div>
      <div class="metric"><strong data-count="4">0</strong><span>UK nations supported through active project delivery</span></div>
      <div class="metric"><strong>72 hrs</strong><span>Typical lead time for standard appointments</span></div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: 5.7rem;
    position: relative;
    isolation: isolate;
  }
  .hero-motion {
    position: absolute;
    inset: 0;
    z-index: -1;
    overflow: hidden;
    border-bottom: 1px solid rgba(205, 218, 231, 0.75);
    background: linear-gradient(180deg, #ffffff 0%, #f9fbfe 74%, #f6f9fc 100%);
  }
  #hero-field {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: .45;
  }
  .hero-grid-fade {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.7), rgba(255, 255, 255, 0.5) 55%, rgba(255, 255, 255, 0.82)),
      linear-gradient(to right, rgba(80, 109, 144, 0.05) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(80, 109, 144, 0.05) 1px, transparent 1px);
    background-size: auto, 88px 88px, 88px 88px;
    mask-image: linear-gradient(180deg, rgba(0,0,0,.9), rgba(0,0,0,.75) 66%, rgba(0,0,0,.4));
  }

  .hero-grid { display: grid; grid-template-columns: 1.2fr .9fr; gap: 1.4rem; align-items: center; }
  .kicker { color: var(--brand); margin: 0; font-weight: 700; letter-spacing: .08em; text-transform: uppercase; font-size: .8rem; }
  h1 { color: var(--text-strong); font-size: clamp(2.2rem, 5vw, 4.4rem); line-height: 1.05; margin: .7rem 0 1.1rem; max-width: 14ch; letter-spacing: -0.03em; }
  .actions { display: flex; flex-wrap: wrap; gap: .8rem; margin-top: 1.45rem; }
  .hero-points { margin: 1.35rem 0 0; padding: 0; list-style: none; display: grid; gap: .45rem; color: var(--muted); }
  .hero-points li { position: relative; padding-left: 1.15rem; }
  .hero-points li::before { content: ''; position: absolute; left: 0; top: .58rem; width: 7px; height: 7px; border-radius: 999px; background: var(--accent); }

  .hero-panel {
    display: grid;
    gap: .85rem;
    border-radius: var(--radius-lg);
    background: linear-gradient(160deg, color-mix(in oklab, #fff 90%, #daeaf8) 0%, color-mix(in oklab, #fff 86%, #dcece8) 100%);
    border: 1px solid var(--border);
    padding: 1rem;
    box-shadow: var(--shadow);
  }
  .metric { padding: 1rem; border-radius: 14px; background: color-mix(in oklab, white 88%, var(--brand-soft)); border: 1px solid #d6e1ec; }
  .metric strong { display: block; color: var(--text-strong); font-size: 1.9rem; line-height: 1.1; letter-spacing: -0.02em; }
  .metric span { color: var(--muted); font-size: .94rem; }


  @media (prefers-reduced-motion: reduce) {
    #hero-field { display: none; }
    .hero-grid-fade { opacity: .7; }
  }

  @media (max-width: 860px) { .hero-grid { grid-template-columns: 1fr; } }
</style>
