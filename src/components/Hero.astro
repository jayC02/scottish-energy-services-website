<section class="section hero">
  <video
    class="hero-video"
    autoplay
    muted
    loop
    playsinline
    aria-hidden="true"
  >
    <source src="/videos/hero-background.mp4" type="video/mp4" />
  </video>
  <div class="hero-overlay" aria-hidden="true"></div>

  <div class="container hero-grid">
    <div data-reveal>
      <p class="kicker">Energy, Compliance & Building Performance</p>
      <h1>Scottish Energy Services</h1>
      <ul class="hero-points">
        <li>Accredited technical services for projects throughout the UK</li>
        <li>Commercially focused advice with practical implementation value</li>
        <li>Fast coordination and report quality you can rely on</li>
      </ul>
      <div class="actions">
        <a class="btn btn-primary" href="/quote">Request a Quote</a>
        <a class="btn btn-ghost" href="/services">Explore Services</a>
      </div>
    </div>
    <div class="hero-panel" data-reveal>
      <div class="metric"><strong data-count="12573">0</strong><span>Projects delivered</span></div>
      <div class="metric"><strong data-count="4">0</strong><span>UK nations supported</span></div>
      <div class="metric"><strong>24 hrs</strong><span>Typical Turnaround Time</span></div>
    </div>
  </div>

  <div class="hero-fade" aria-hidden="true"></div>
</section>

<style>
  .hero {
    padding-top: 5.7rem;
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border-bottom: 1px solid rgba(205, 218, 231, 0.42);
    background: #f2f6fb;
  }

  .hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -3;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: -2;
    background: rgba(0, 0, 0, 0.3);
  }

  .hero-fade {
    position: absolute;
    inset-inline: 0;
    bottom: 0;
    height: clamp(34px, 4vw, 52px);
    pointer-events: none;
    z-index: 2;
    background: linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0) 0%,
      rgba(255, 255, 255, .08) 58%,
      rgba(255, 255, 255, .34) 84%,
      rgba(255, 255, 255, .72) 100%
    );
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1.2fr .9fr;
    gap: 1.4rem;
    align-items: center;
  }

  .kicker {
    color: #d8ecff;
    margin: 0;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
    font-size: .8rem;
    text-shadow: 0 2px 14px rgba(5, 17, 30, 0.45);
  }

  h1 {
    color: #ffffff;
    font-size: clamp(2.2rem, 5vw, 4.4rem);
    line-height: 1.05;
    margin: .7rem 0 1.1rem;
    max-width: 14ch;
    letter-spacing: -0.03em;
    text-shadow: 0 5px 24px rgba(6, 15, 25, 0.45);
  }

  .hero :global(.lead) {
    color: rgba(245, 249, 255, 0.94);
    text-shadow: 0 3px 16px rgba(6, 15, 25, 0.38);
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: .8rem;
    margin-top: 1.45rem;
  }

  .hero-points {
    margin: 1.35rem 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: .45rem;
    color: rgba(236, 243, 251, 0.94);
    text-shadow: 0 2px 14px rgba(5, 17, 30, 0.38);
  }

  .hero-points li {
    position: relative;
    padding-left: 1.15rem;
  }

  .hero-points li::before {
    content: '';
    position: absolute;
    left: 0;
    top: .58rem;
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: #76e0c2;
  }

  .hero-panel {
    display: grid;
    justify-self: end;
    width: min(100%, 320px);
    gap: .65rem;
    padding: 0;
  }

  .metric {
    --stat-glow-x: 50%;
    --stat-glow-y: 50%;
    --tilt-x: 0deg;
    --tilt-y: 0deg;
    position: relative;
    overflow: hidden;
    padding: .7rem 0 .7rem .95rem;
    border-left: 2px solid rgba(118, 224, 194, .68);
    background: linear-gradient(90deg, rgba(255, 255, 255, .09), rgba(255, 255, 255, .025) 48%, rgba(255, 255, 255, 0));
    transform: perspective(720px) rotateX(var(--tilt-x)) rotateY(var(--tilt-y));
    transition: transform .25s ease, background .25s ease, border-color .25s ease;
  }

  .metric::before {
    content: "";
    position: absolute;
    inset: -70%;
    pointer-events: none;
    opacity: 0;
    background: radial-gradient(circle at var(--stat-glow-x) var(--stat-glow-y), rgba(118, 224, 194, .18), transparent 30%);
    transition: opacity .25s ease;
  }

  .metric:hover {
    border-left-color: #76e0c2;
    background: linear-gradient(90deg, rgba(255, 255, 255, .14), rgba(255, 255, 255, .04) 48%, rgba(255, 255, 255, 0));
  }

  .metric:hover::before {
    opacity: 1;
  }

  .metric strong {
    position: relative;
    display: block;
    color: #ffffff;
    font-size: clamp(1.65rem, 2.2vw, 2.15rem);
    line-height: 1.1;
    letter-spacing: 0;
    text-shadow: 0 2px 14px rgba(5, 17, 30, .42);
  }

  .metric span {
    position: relative;
    color: rgba(245, 249, 255, .8);
    font-size: .9rem;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-video {
      display: none;
    }

    .hero {
      background: linear-gradient(180deg, #19466b 0%, #0f2b44 56%, #f2f6fb 100%);
    }

    .metric {
      transform: none !important;
      transition: none;
    }

    .metric::before {
      display: none;
    }
  }

  @media (max-width: 860px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-panel {
      justify-self: start;
      width: min(100%, 320px);
    }

    .metric {
      padding: .65rem 0 .65rem .85rem;
    }

    .hero-fade {
      height: 38px;
    }
  }
</style>
