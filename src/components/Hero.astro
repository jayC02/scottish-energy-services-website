<section class="section hero">
  <video
    class="hero-video"
    autoplay
    muted
    loop
    playsinline
    aria-hidden="true"
  >
    <source src="/videos/hero-background.mp4" type="video/mp4" />
  </video>
  <div class="hero-overlay" aria-hidden="true"></div>

  <div class="container hero-grid">
    <div data-reveal>
      <p class="kicker">Energy, Compliance & Building Performance</p>
      <h1>Scottish Energy Services</h1>
      <p class="lead">Based in Scotland and delivering projects across the UK, we support residential and non-domestic clients with disciplined assessments, clear reporting, and dependable communication.</p>
      <div class="actions">
        <a class="btn btn-primary" href="/quote">Request a Quote</a>
        <a class="btn btn-ghost" href="/services">Explore Services</a>
      </div>
      <ul class="hero-points">
        <li>Accredited technical services for projects throughout the UK</li>
        <li>Commercially focused advice with practical implementation value</li>
        <li>Fast coordination and report quality you can rely on</li>
      </ul>
    </div>
    <div class="hero-panel" data-reveal>
      <div class="metric"><strong data-count="3200">0</strong><span>Projects delivered</span></div>
      <div class="metric"><strong data-count="4">0</strong><span>UK nations supported through active project delivery</span></div>
      <div class="metric"><strong>72 hrs</strong><span>Typical lead time for standard appointments</span></div>
    </div>
  </div>

  <div class="hero-fade" aria-hidden="true"></div>
</section>

<style>
  .hero {
    padding-top: 5.7rem;
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border-bottom: 1px solid rgba(205, 218, 231, 0.42);
    background: #f2f6fb;
  }

  .hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -3;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: -2;
    background: rgba(0, 0, 0, 0.3);
  }

  .hero-fade {
    position: absolute;
    inset-inline: 0;
    bottom: 0;
    height: clamp(120px, 18vw, 190px);
    pointer-events: none;
    z-index: 2;
    background: linear-gradient(to bottom, rgba(242, 246, 251, 0), #ffffff 84%);
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1.2fr .9fr;
    gap: 1.4rem;
    align-items: center;
  }

  .kicker {
    color: #d8ecff;
    margin: 0;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
    font-size: .8rem;
    text-shadow: 0 2px 14px rgba(5, 17, 30, 0.45);
  }

  h1 {
    color: #ffffff;
    font-size: clamp(2.2rem, 5vw, 4.4rem);
    line-height: 1.05;
    margin: .7rem 0 1.1rem;
    max-width: 14ch;
    letter-spacing: -0.03em;
    text-shadow: 0 5px 24px rgba(6, 15, 25, 0.45);
  }

  .hero :global(.lead) {
    color: rgba(245, 249, 255, 0.94);
    text-shadow: 0 3px 16px rgba(6, 15, 25, 0.38);
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: .8rem;
    margin-top: 1.45rem;
  }

  .hero-points {
    margin: 1.35rem 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: .45rem;
    color: rgba(236, 243, 251, 0.94);
    text-shadow: 0 2px 14px rgba(5, 17, 30, 0.38);
  }

  .hero-points li {
    position: relative;
    padding-left: 1.15rem;
  }

  .hero-points li::before {
    content: '';
    position: absolute;
    left: 0;
    top: .58rem;
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: #76e0c2;
  }

  .hero-panel {
    display: grid;
    gap: .85rem;
    border-radius: var(--radius-lg);
    background: linear-gradient(160deg, rgba(255, 255, 255, 0.86) 0%, rgba(236, 245, 254, 0.78) 100%);
    border: 1px solid rgba(218, 230, 244, 0.82);
    padding: 1rem;
    box-shadow: 0 24px 64px rgba(13, 28, 44, 0.32);
    backdrop-filter: blur(4px);
  }

  .metric {
    padding: 1rem;
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.76);
    border: 1px solid rgba(214, 225, 236, 0.8);
  }

  .metric strong {
    display: block;
    color: var(--text-strong);
    font-size: 1.9rem;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .metric span {
    color: var(--muted);
    font-size: .94rem;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-video {
      display: none;
    }

    .hero {
      background: linear-gradient(180deg, #19466b 0%, #0f2b44 56%, #f2f6fb 100%);
    }
  }

  @media (max-width: 860px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-fade {
      height: 135px;
    }
  }
</style>
