<div class="trust-strip">
  <div class="container trust-grid">
    {[
      "Accredited assessments",
      "Commercial & public-sector experience",
      "Single sites & multi-site portfolios",
      "UK-wide coverage",
    ].map((t) => (
      <p>
        <span aria-hidden="true">✓</span>
        {t}
      </p>
    ))}
  </div>
</div>
<style>
  .trust-strip {
    border-bottom: 1px solid var(--border);
  }
  .trust-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    padding-block: 1.7rem;
    gap: 1.5rem;
  }
  .trust-grid p {
    font-size: 0.75rem;
    margin: 0;
    display: flex;
    gap: 0.7rem;
    align-items: center;
  }
  .trust-grid span {
    color: var(--brand);
  }
  @media (max-width: 760px) {
    .trust-grid {
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }
  }
</style>
