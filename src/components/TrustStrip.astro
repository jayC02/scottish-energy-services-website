<section class="trust-section" aria-label="Scottish Energy Services credentials">
  <div class="container trust-rail" data-reveal>
    <div class="trust-item">
      <span class="trust-marker" aria-hidden="true">01</span>
      <div>
        <strong>Accredited assessments</strong>
        <span>Delivered to recognised standards</span>
      </div>
    </div>

    <div class="trust-item">
      <span class="trust-marker" aria-hidden="true">02</span>
      <div>
        <strong>Domestic & commercial</strong>
        <span>Single properties to portfolio programmes</span>
      </div>
    </div>

    <div class="trust-item">
      <span class="trust-marker" aria-hidden="true">03</span>
      <div>
        <strong>Scotland-based</strong>
        <span>Project delivery across the UK</span>
      </div>
    </div>

    <div class="trust-item">
      <span class="trust-marker" aria-hidden="true">04</span>
      <div>
        <strong>Clear technical reporting</strong>
        <span>Practical outputs built for action</span>
      </div>
    </div>
  </div>
</section>

<style>
  .trust-section {
    border-top: 1px solid #e0e7ef;
    border-bottom: 1px solid #e0e7ef;
    background: #f8fafc;
  }

  .trust-rail {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .trust-item {
    position: relative;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: .8rem;
    align-items: start;
    min-height: 116px;
    padding: 1.45rem 1.5rem;
  }

  .trust-item + .trust-item {
    border-left: 1px solid #dbe4ed;
  }

  .trust-marker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    margin-top: .05rem;
    border-radius: 7px;
    background: var(--brand-soft);
    color: var(--brand-strong);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: .7rem;
    font-weight: 700;
  }

  .trust-item strong {
    display: block;
    margin-bottom: .2rem;
    color: var(--text-strong);
    font-size: .98rem;
    line-height: 1.3;
  }

  .trust-item div > span {
    color: var(--muted);
    font-size: .87rem;
    line-height: 1.45;
  }

  @media (max-width: 980px) {
    .trust-rail {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .trust-item:nth-child(3) {
      border-left: 0;
      border-top: 1px solid #dbe4ed;
    }

    .trust-item:nth-child(4) {
      border-top: 1px solid #dbe4ed;
    }
  }

  @media (max-width: 640px) {
    .trust-rail {
      grid-template-columns: 1fr;
    }

    .trust-item {
      min-height: 0;
      padding: 1.05rem 0;
    }

    .trust-item + .trust-item,
    .trust-item:nth-child(3),
    .trust-item:nth-child(4) {
      border-left: 0;
      border-top: 1px solid #dbe4ed;
    }
  }
</style>
